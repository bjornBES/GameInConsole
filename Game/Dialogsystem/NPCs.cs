
namespace GameInConsole.Game.Dialogsystem
{
    public class NPCEntry
    {
        public string Name;
        public string ResourceKey;

        public NPCEntry(string name, string resourceKey)
        {
            Name = name;
            ResourceKey = resourceKey;
        }
    }
}